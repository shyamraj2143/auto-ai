# Production uses the frontend origin and nginx same-origin proxy.
# Browser requests must go to https://autoai.site.je/api/v1, not directly to Railway.
VITE_API_URL=/api/v1
VITE_CRYSTAL_UI_ENABLED=true
VITE_ENABLE_ADVANCED_MOTION=true
VITE_ENABLE_CINEMATIC_WEBSITE=true
