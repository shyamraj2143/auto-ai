import type { ReactNode } from "react";
export function RequestsPanel({ children }: { children: ReactNode }) { return <section className="calls-list social-panel connections-panel" aria-label="Connection requests">{children}</section>; }
