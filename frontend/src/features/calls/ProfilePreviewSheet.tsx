import type { ReactNode } from "react";
export function ProfilePreviewSheet({ children }: { children: ReactNode }) { return <aside className="social-profile-panel profile-preview-sheet" aria-label="Profile preview">{children}</aside>; }
