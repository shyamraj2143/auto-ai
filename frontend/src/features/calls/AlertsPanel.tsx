import type { ReactNode } from "react";
export function AlertsPanel({ children }: { children: ReactNode }) { return <section className="calls-list social-panel alerts-panel" aria-label="Alerts">{children}</section>; }
