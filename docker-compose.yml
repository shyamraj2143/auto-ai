services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    environment:
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      RELATIONSHIP_FOLLOWUP_WORKER_ENABLED: "false"
    ports:
      - "8000:8000"
    volumes:
      - ./database:/app/database
      - ./backend/uploads:/app/backend/uploads
    restart: unless-stopped
    depends_on:
      - redis

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_URL: /api/v1
    depends_on:
      - backend
    ports:
      - "5173:80"
    restart: unless-stopped

  relationship-worker:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    environment:
      RELATIONSHIP_FOLLOWUP_WORKER_ENABLED: "false"
    command: ["python", "-m", "app.workers.relationship_followups"]
    volumes:
      - ./database:/app/database
    restart: unless-stopped
    depends_on:
      - redis

  redis:
    image: redis:7.4-alpine
    command: redis-server --appendonly yes
    volumes:
      - auto_ai_redis:/data
    restart: unless-stopped

volumes:
  auto_ai_redis:
