Auto-AI critical native calling release

This release must replace older APKs because the single incoming-call surface,
non-terminal Answer handoff, ringtone cleanup, and native video renderer repair
are compiled into the Android application.
